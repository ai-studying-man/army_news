# 구현 TODOLIST

이 문서는 의존 관계 순서대로 진행한다. 현재 완료 범위는 프로젝트 뼈대와 설계 문서뿐이다.

## 0. 수용 요구사항 기준

- [x] DAPA News와 수집·발행·Telegram 전송 아키텍처를 공유하되 Army News의 주제와 수집
      시간은 별도로 관리
- [x] 별칭 `육군`, `8사단`, `8기동사단`, `3070부대`, `오뚜기부대`와 지역 `양주`, `동두천`,
      `포천`, `연천`, `의정부` 문서화
- [x] 지역 범위에 음주 사건, 자연재해, 육군 관련 지자체 업무·행사 포함
- [x] 수집 구간 D-1 14:00 KST–D-day 05:00 KST, 발행 06:30 KST로 고정
- [x] 동일 사건은 대표 원문 기사 한 건만 전송
- [x] 항목은 `■ [사단] 기사 제목 (신문명)` 또는 `■ [지역] 기사 제목 (신문명)`, URL,
      `- 한두문장으로 기사 요약`의 정확한 세 줄(항목 사이 빈 줄) 사용
- [x] Bot Token과 Chat ID는 절대 커밋하지 않고 GitHub Secrets로만 관리
- [x] 채팅에 노출된 토큰은 운영 전에 회전하고, `chat_id`가 없으면 실제 전송하지 않음

## 1. 기반 설계

- [x] Python 3.12, `uv`, src-layout 패키지 메타데이터 정의
- [x] 공개 HTTPS 전용, 결정론적·발췌식 요약, 민감 정보 비추론 원칙 문서화
- [x] D-1 14:00 KST부터 D-day 05:00 KST까지의 수집 창과 06:30 KST 발행 시각 확정
- [ ] 설정 스키마에 사단 정식 명칭, 별칭, 담당 지역, 출처와 우선순위 정의
- [ ] 설정 fixture와 잘못된 URL·중복 별칭·시간대 누락 검증 테스트 작성

## 2. 테스트 우선 핵심 모델

- [ ] 기사, 출처, 사건, 카테고리, 브리핑 모델의 실패 테스트 작성
- [ ] 14:00 시작·05:00 종료 경계, 06:30 예약 실행의 경계 테스트 작성
- [ ] HTTPS가 아닌 URL과 비공개 출처 거부 테스트 작성
- [ ] 최소 모델과 KST 시간 창 계산 구현

## 3. 수집과 정규화

- [ ] 공식 공개 출처와 RSS별 로컬 fixture 작성
- [ ] `httpx` 타임아웃, 제한 재시도, User-Agent와 출처별 오류 격리 테스트 작성
- [ ] RSS/공개 페이지 수집기와 URL 정규화 구현
- [ ] 발행 시각 누락·잘못된 XML·리디렉션·일부 출처 실패 회귀 테스트 작성

## 4. 분류와 안전 필터

- [ ] 포함, 제외, 동명이인, 숫자 사단명, 지역 오탐 fixture 작성
- [ ] 별칭·지역과 육군 업무 맥락을 함께 평가하는 결정론적 규칙 구현
- [ ] 부대 위치·이동·병력 규모·장비 배치 등 민감 추론 방지 테스트와 필터 구현
- [ ] 카테고리별 최대 5건 및 부족분 미충원 테스트 작성

## 5. 사건 중복 제거와 대표 기사

- [ ] 같은 사건의 제목 변형·재전재 fixture 작성
- [ ] 계약 상태, 국가, 시험/양산 등 비중복 경계 fixture 작성
- [ ] 사건 키 생성, 대표 기사 우선순위와 안정 정렬 구현
- [ ] 동일 입력의 결과가 반복 실행에서 같은지 검증

## 6. 발췌식 브리핑

- [ ] 모든 출력 문장이 단일 선택 원문에서 추적되는 테스트 작성
- [ ] `■ [사단] ...`/`■ [지역] ...`, URL, 요약의 세 줄 항목 형식과 빈 줄 구분 테스트 작성
- [ ] HTML escape, 메시지 길이와 원문 HTTPS 링크 테스트 작성
- [ ] 결정론적 발췌, 업무 참고와 Telegram HTML 렌더링 구현
- [ ] 여러 기사를 결합하거나 원문에 없는 사실을 생성하지 않는 회귀 테스트 작성

## 7. CLI와 전송

- [ ] 자격 증명 없이 `--dry-run`이 성공하고 전송 호출이 없는 테스트 작성
- [ ] dry-run 결과 파일과 구조화 로그 구현
- [ ] 환경 변수 기반 Telegram 전송, 비밀 마스킹과 오류 처리 구현
- [ ] Bot Token·Chat ID의 GitHub Secrets 주입, chat_id 없을 때 전송 차단, 노출 토큰 회전 절차
      문서화·검증
- [x] 같은 날짜 예약 메시지 중복 방지 및 수동 실행 명시 동작 구현

## 8. 자동화와 최종 검증

- [x] 06:30 KST에 해당하는 GitHub Actions cron 및 `workflow_dispatch` 작성
- [x] 예약/수동 실행 구분과 Secrets 이름의 정적 테스트 작성
- [x] `uv run pytest` 실행
- [x] `uv run ruff check .` 및 `uv run ruff format --check .` 실행
- [x] `uv run basedpyright` 실행
- [ ] 자격 증명 없는 로컬 dry-run 검토
- [ ] 승인 뒤 GitHub Secrets 등록 및 Telegram 시험 전송
