# Keep blank for collection-only dry runs. Never commit real bot tokens or chat IDs.
# In production, store both values as GitHub Secrets. Do not send until chat_id exists.
# Rotate/revoke any token exposed in chat before production use.
# Optional JSON override for division name, aliases, and regions. Source URLs/priorities remain
# code-reviewed in src/army_morning_brief/sources.py. Example (safe synthetic values only):
# ARMY_BRIEF_CONFIG_JSON={"divisions":[{"name":"제8기동사단","aliases":["육군","8사단"],"regions":["양주","동두천"]}]}
ARMY_BRIEF_CONFIG_JSON=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
