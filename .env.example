# Keep blank for collection-only dry runs. Never commit real bot tokens or chat IDs.
# In production, store both values as GitHub Secrets. Do not send until chat_id exists.
# Rotate/revoke any token exposed in chat before production use.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
